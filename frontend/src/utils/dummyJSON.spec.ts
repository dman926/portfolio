describe("dummyJSON", () => {
  /**
   * TODO:
   * The function itself is dead simple
   * Tests are mostly for type checking. dummyJSON.ts is 90% types
   */

  it("passes", () => {
    expect(true).toBe(true);
  });
});
